---
import { getCollection, render } from 'astro:content';
import type { GetStaticPaths } from 'astro';
import BaseLayout from '@/layouts/BaseLayout.astro';
import Header from '@/components/layout/Header.astro';
import Footer from '@/components/layout/Footer.astro';
import Badge from '@/components/ui/Badge.astro';
import { getPillarLabel } from '@/data/pillars';

export const getStaticPaths = (async () => {
  const projects = await getCollection('projects');
  return projects.map(project => ({
    params: { slug: project.id },
    props: { project },
  }));
}) satisfies GetStaticPaths;

const { project } = Astro.props;
const { Content } = await render(project);

---

<BaseLayout title={project.data.title} description={project.data.description}>
  <Header lang="it" />

  <main class="max-w-5xl mx-auto px-5 py-8">
    <!-- Breadcrumb -->
    <nav class="text-xs text-text-muted mb-6" aria-label="Breadcrumb">
      <ol class="flex items-center gap-1.5">
        <li><a href="/" class="hover:text-accent transition-colors">Home</a></li>
        <li aria-hidden="true" class="opacity-40">/</li>
        <li><a href="/progetti/" class="hover:text-accent transition-colors">Progetti</a></li>
        <li aria-hidden="true" class="opacity-40">/</li>
        <li class="truncate max-w-[200px] text-text-dark dark:text-text-light" title={project.data.title}>{project.data.title}</li>
      </ol>
    </nav>

    <!-- Title block -->
    <header class="mb-10 max-w-3xl">
      <div class="flex items-center gap-3 mb-3">
        {project.data.type === 'workshop' && (
          <span class="text-[10px] uppercase font-medium tracking-wide text-accent bg-accent/10 px-2 py-0.5 rounded-full">Workshop</span>
        )}
        <Badge pillar={project.data.pillar}>
          {getPillarLabel(project.data.pillar)}
        </Badge>
      </div>
      <h1 class="text-3xl md:text-4xl font-bold leading-tight tracking-tight text-text-dark dark:text-text-light mb-4">
        {project.data.title}
      </h1>
      <p class="text-text-muted text-lg leading-relaxed mb-4">{project.data.description}</p>
      {project.data.tags.length > 0 && (
        <div class="flex flex-wrap gap-1.5">
          {project.data.tags.map(tag => (
            <span class="text-[11px] text-text-muted/80 bg-text-muted/8 px-1.5 py-0.5 rounded">{tag}</span>
          ))}
        </div>
      )}
    </header>

    <!-- Links -->
    {project.data.links && (
      <div class="flex flex-wrap gap-4 mb-10">
        {project.data.links.github && (
          <a
            href={project.data.links.github}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center gap-2 px-4 py-2 rounded-lg border border-border dark:border-border-dark text-sm font-medium hover:border-accent hover:text-accent transition-colors"
          >
            <svg class="w-4 h-4" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg>
            GitHub
          </a>
        )}
        {project.data.links.demo && (
          <a
            href={project.data.links.demo}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center gap-2 px-4 py-2 rounded-lg bg-accent text-white text-sm font-medium hover:bg-accent-hover transition-colors"
          >
            Demo &rarr;
          </a>
        )}
        {project.data.links.blog && (
          <a
            href={project.data.links.blog}
            class="inline-flex items-center gap-2 px-4 py-2 rounded-lg border border-border dark:border-border-dark text-sm font-medium hover:border-accent hover:text-accent transition-colors"
          >
            Articoli correlati &rarr;
          </a>
        )}
      </div>
    )}

    <!-- Content -->
    <article class="prose prose-lg dark:prose-invert max-w-3xl
      prose-headings:tracking-tight
      prose-h2:text-2xl prose-h2:font-bold prose-h2:mt-12 prose-h2:mb-4
      prose-h3:text-xl prose-h3:font-semibold prose-h3:mt-8 prose-h3:mb-3
      prose-p:leading-relaxed prose-p:mb-4
      prose-a:text-accent prose-a:no-underline hover:prose-a:underline prose-a:underline-offset-4
      prose-img:rounded-xl prose-img:shadow-sm prose-img:border prose-img:border-border/50
      prose-code:before:content-none prose-code:after:content-none
      prose-pre:bg-code-bg prose-pre:rounded-xl
      prose-blockquote:border-l-accent prose-blockquote:bg-accent/5 prose-blockquote:rounded-r-lg prose-blockquote:py-1
      prose-li:marker:text-accent">
      <Content />
    </article>
  </main>

  <Footer lang="it" />
</BaseLayout>
