---
import PageLayout from '@/layouts/PageLayout.astro';
import PageHero from '@/components/ui/PageHero.astro';
import BlogFilterable from '@/components/blog/BlogFilterable.vue';
import { postHref, estimateReadingTime } from '@/utils/blog';

interface Props {
  allPosts: any[];
  title: string;
  description: string;
  lang?: 'it' | 'en';
  ogImage?: string;
}

const { allPosts, title, description, lang = 'it', ogImage } = Astro.props;

const totalCount = allPosts.length;

const t = lang === 'en'
  ? { articles: 'articles' }
  : { articles: 'articoli' };

// Serialize ALL posts sorted by date descending for the Vue component
const serializedPosts = allPosts
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime())
  .map(post => ({
    id: post.id,
    title: post.data.title,
    description: post.data.description,
    date: post.data.date.toISOString(),
    pillar: post.data.pillar,
    category: post.data.category,
    tags: post.data.tags,
    series: post.data.series,
    href: postHref(post.id, lang),
    readingTime: estimateReadingTime(post.body),
  }));
---

<PageLayout title={title} description={description} lang={lang} ogImage={ogImage}>
  <PageHero
    slot="hero"
    title={title}
    subtitle={`${description}. ${totalCount} ${t.articles}.`}
  />

  <div class="fade-in-section">
    <BlogFilterable client:load posts={serializedPosts} lang={lang} />
  </div>
</PageLayout>
